type Query {
  ordersByCustomer(customerId: ID!): [Order!]!
  ratingsByOrder(orderId: ID!): [Rating!]!
}

type Mutation {
  submitRating(input: RatingInput!): Rating!
}

input RatingInput {
  orderId: ID!
  customerId: ID!
  ratingScore: Int!
  review: String
}

type Rating {
  id: ID!
  ratingScore: Int!
  review: String
  orderId: ID!
  customerId: ID!
}

type Order {
  id: ID!
  status: String!
  orderDate: String
  deliveryAddress: String
  totalAmount: Float
  items: [OrderLine!]!
}

type OrderLine {
  bouquetName: String
  quantity: Int
  price: Float
}
