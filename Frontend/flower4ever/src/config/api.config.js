export const API_MODE = "REST"; //or "GRAPHQL"

export const API_BASE_URL = "http://localhost:8080";

export const REST_BASE = `${API_BASE_URL}/api/v1`;
export const GRAPHQL_BASE = `${API_BASE_URL}/graphql`;
